{
  "name": "@smartpatrol/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate": "drizzle-kit generate",
    "migrate:local": "wrangler d1 migrations apply smartpatrol --local",
    "migrate:remote": "wrangler d1 migrations apply smartpatrol --remote"
  },
  "dependencies": {
    "@smartpatrol/contracts": "workspace:*",
    "drizzle-orm": "^0.38.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "drizzle-kit": "^0.30.1",
    "wrangler": "^3.99.0"
  }
}
