{
  "name": "@smartpatrol/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "create-admin": "tsx scripts/create-admin.ts"
  },
  "dependencies": {
    "@noble/hashes": "^1.6.1",
    "@smartpatrol/contracts": "workspace:*",
    "@smartpatrol/db": "workspace:*",
    "drizzle-orm": "^0.38.2",
    "hono": "^4.6.14",
    "jose": "^5.9.6",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "tsx": "^4.19.2",
    "vitest": "^2.1.8",
    "wrangler": "^3.99.0"
  }
}
