# application

Use-cases (interactors) and ports (repository/gateway interfaces). Orchestrates the
domain; depends on `domain` only. Infrastructure implements the ports defined here.

Depends on: `domain`.
