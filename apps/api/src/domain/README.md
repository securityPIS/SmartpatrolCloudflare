# domain

Pure business entities, value objects, and domain services. **No IO** — no D1, no
fetch, no Workers globals. Everything here must be trivially unit-testable.

Depends on: nothing (innermost layer).
